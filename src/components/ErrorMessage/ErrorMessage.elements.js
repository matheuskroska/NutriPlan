import styled from "styled-components";

export const StyledError = styled.div`
    color: var(--font-dark);
    display: flex;
    align-items: center;
    font-size: 1.3em;
    font-weight: 600;
    gap: 0 10px;
`
