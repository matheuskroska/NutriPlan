import {NavContainer,NavWrapper} from './Header.elements'

export const Header = () => {
    return (
            <NavContainer>
                <NavWrapper></NavWrapper>
            </NavContainer>
    )
}